## 0.1.2

- Fixed KLEY and other operation-specific forms failing with `Could not get parameter` when unrelated optional fields are hidden in n8n.
- Added regression coverage for KLEY `Change Speed` payload generation.
